const { addPostToDb } = require("./queries.js");

const createNewPost = async (req, res, next) => {
  try {
    // TODO: add new post logic here
  } catch (err) {
    console.error("Error creating the new post.", err);
  }
};
